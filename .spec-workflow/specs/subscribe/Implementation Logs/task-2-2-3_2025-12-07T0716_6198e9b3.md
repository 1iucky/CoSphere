# Implementation Log: Task 2.2.3

**Summary:** 实现使用量模型 model/subscription_usage.go，核心功能是滚动窗口额度追踪，支持 5小时/天/周/月 周期，包含额度消费、退款、检查等操作

**Timestamp:** 2025-12-07T07:16:24.445Z
**Log ID:** 6198e9b3-af48-4984-a0d3-45fdd4ee178f

---

## Statistics

- **Lines Added:** +420
- **Lines Removed:** -0
- **Files Changed:** 1
- **Net Change:** 420

## Files Modified
_No files modified_

## Files Created
- model/subscription_usage.go

---

## Artifacts

### 数据结构

#### SubscriptionUsage 结构体

```go
type SubscriptionUsage struct {
    Id             int64  `json:"id" gorm:"primaryKey;autoIncrement"`
    SubscriptionId int64  `json:"subscription_id" gorm:"not null;uniqueIndex:idx_subscription_usage_unique,priority:1"`
    Period         string `json:"period" gorm:"type:varchar(32);not null;uniqueIndex:idx_subscription_usage_unique,priority:2"`
    WindowStart    int64  `json:"window_start" gorm:"bigint;not null;uniqueIndex:idx_subscription_usage_unique,priority:3"`
    WindowEnd      int64  `json:"window_end" gorm:"bigint;not null"`
    UsedQuota      int64  `json:"used_quota" gorm:"bigint;not null;default:0"`
    LimitQuota     int64  `json:"limit_quota" gorm:"bigint;not null"`
}
```

| 字段 | 类型 | 说明 |
|-----|------|------|
| Period | string | 周期类型：five_hours/day/week/month |
| WindowStart | int64 | 窗口开始时间（时间戳） |
| WindowEnd | int64 | 窗口结束时间（时间戳） |
| UsedQuota | int64 | 已使用额度 |
| LimitQuota | int64 | 限额上限 |

### 实现的方法

#### CRUD 方法
| 方法名 | 签名 | 说明 |
|-------|------|------|
| `CreateSubscriptionUsage` | `(usage *SubscriptionUsage) error` | 创建使用量记录 |
| `GetOrCreateSubscriptionUsage` | `(subscriptionId int64, period string, limitQuota int64) (*SubscriptionUsage, error)` | 获取或创建当前窗口记录 |
| `GetCurrentUsageBySubscription` | `(subscriptionId int64) ([]*SubscriptionUsage, error)` | 获取当前所有周期使用量 |
| `GetUsageBySubscriptionAndPeriod` | `(subscriptionId int64, period string) (*SubscriptionUsage, error)` | 获取指定周期使用量 |

#### 额度操作方法
| 方法名 | 签名 | 说明 |
|-------|------|------|
| `ConsumeQuota` | `(subscriptionId int64, period string, amount int64, limitQuota int64) error` | 消费额度（原子） |
| `CheckAndConsumeQuota` | `(subscriptionId int64, period string, amount int64, limitQuota int64) (bool, error)` | 检查并消费 |
| `GetRemainingQuota` | `(subscriptionId int64, period string) (int64, error)` | 获取剩余额度 |
| `RefundQuota` | `(usageId int64, amount int64) error` | 退还额度 |

#### 窗口计算方法
| 方法名 | 签名 | 说明 |
|-------|------|------|
| `CalculateWindowBounds` | `(period string, timestamp int64) (windowStart, windowEnd int64)` | 计算窗口边界 |
| `GetWindowDurationSeconds` | `(period string) int64` | 获取窗口时长（秒） |

### 关键实现

#### 滚动窗口计算
```go
func CalculateWindowBounds(period string, timestamp int64) (windowStart, windowEnd int64) {
    t := time.Unix(timestamp, 0).UTC()
    switch period {
    case common.LimitPeriodFiveHours:
        hour := t.Hour()
        windowHour := (hour / 5) * 5
        windowStart = time.Date(t.Year(), t.Month(), t.Day(), windowHour, 0, 0, 0, time.UTC).Unix()
        windowEnd = windowStart + 5*60*60 - 1
    case common.LimitPeriodDay:
        windowStart = time.Date(t.Year(), t.Month(), t.Day(), 0, 0, 0, 0, time.UTC).Unix()
        windowEnd = windowStart + 24*60*60 - 1
    // ... week/month similar
    }
    return windowStart, windowEnd
}
```

#### 原子额度消费
```go
result := tx.Model(&SubscriptionUsage{}).
    Where("id = ? AND used_quota + ? <= limit_quota", usage.Id, amount).
    Update("used_quota", gorm.Expr("used_quota + ?", amount))
if result.RowsAffected == 0 {
    return errors.New(common.MsgSubscriptionQuotaExhausted)
}
```

#### UPSERT 并发安全
```go
err = DB.Clauses(clause.OnConflict{
    Columns:   []clause.Column{{Name: "subscription_id"}, {Name: "period"}, {Name: "window_start"}},
    DoNothing: true,
}).Create(&usage).Error
```

### 周期类型
| 周期 | 常量 | 窗口时长 | 说明 |
|------|------|---------|------|
| 5小时 | `five_hours` | 5小时 | 以整5小时为边界 |
| 天 | `day` | 24小时 | UTC 00:00 开始 |
| 周 | `week` | 7天 | 周一 00:00 开始 |
| 月 | `month` | 变长 | 每月1号 00:00 开始 |

### 索引设计
| 索引名 | 字段 | 说明 |
|-------|------|------|
| `idx_subscription_usage_unique` | (subscription_id, period, window_start) | 唯一约束 |

### 依赖常量
- `common.LimitPeriodFiveHours` / `Day` / `Week` / `Month`
- `common.MsgSubscriptionQuotaExhausted`

---

## 二次修复 (2025-12-08T02:26)

### 修复的问题

#### 1. 旧方法未适配窗口策略 ✓ 已修复

**问题**: GetOrCreateSubscriptionUsage、ConsumeQuota 等旧方法仍使用固定窗口 CalculateWindowBounds，忽略 window_strategy 参数及"首次实际使用触发滚动窗口"规则（需求6.7、任务2.2.8）

**修复**: 将旧方法标记为弃用，并委托给新的策略感知方法

```go
// GetOrCreateSubscriptionUsage:50-55（已弃用，委托给新方法）
func GetOrCreateSubscriptionUsage(subscriptionId int64, period string, limitQuota int64) (*SubscriptionUsage, error) {
    // 默认使用滚动窗口策略
    return GetOrCreateActiveUsageWindow(subscriptionId, period, limitQuota, common.WindowStrategyRolling)
}

// GetOrCreateSubscriptionUsageWithTx:57-62（已弃用，委托给新方法）
func GetOrCreateSubscriptionUsageWithTx(tx *gorm.DB, subscriptionId int64, period string, limitQuota int64) (*SubscriptionUsage, error) {
    return GetOrCreateActiveUsageWindowWithTx(tx, subscriptionId, period, limitQuota, common.WindowStrategyRolling)
}

// ConsumeQuota:268-275（已弃用，委托给新方法）
func ConsumeQuota(subscriptionId int64, period string, amount int64, limitQuota int64) error {
    return ConsumeQuotaWithStrategy(subscriptionId, period, amount, limitQuota, common.WindowStrategyRolling)
}

// CheckAndConsumeQuota:320-333（已弃用，委托给新方法）
func CheckAndConsumeQuota(subscriptionId int64, period string, amount int64, limitQuota int64) (bool, error) {
    return CheckAndConsumeQuotaWithStrategy(subscriptionId, period, amount, limitQuota, common.WindowStrategyRolling)
}
```

#### 2. 缺少 Update/ResetWindow/AtomicIncrement 方法 ✓ 已修复

**问题**: 缺少任务2.2.3提到的 UpdateUsageWindow、ResetUsageWindow、AtomicIncrementUsage 方法

**修复**: 添加完整的窗口管理和原子操作方法

```go
// UpdateUsageWindow:732-750 - 更新使用量窗口
func UpdateUsageWindow(usageId int64, usedQuota int64, limitQuota int64) error {
    if usageId == 0 {
        return errors.New("使用量记录 ID 不能为空")
    }
    updates := map[string]interface{}{}
    if usedQuota >= 0 {
        updates["used_quota"] = usedQuota
    }
    if limitQuota > 0 {
        updates["limit_quota"] = limitQuota
    }
    if len(updates) == 0 {
        return errors.New("没有可更新的字段")
    }
    return DB.Model(&SubscriptionUsage{}).Where("id = ?", usageId).Updates(updates).Error
}

// UpdateUsageWindowWithTx:752-770 - 事务版本
func UpdateUsageWindowWithTx(tx *gorm.DB, usageId int64, usedQuota int64, limitQuota int64) error {
    // ... 实现与上同
}

// ResetUsageWindow:774-784 - 重置使用量窗口
func ResetUsageWindow(usageId int64) error {
    if usageId == 0 {
        return errors.New("使用量记录 ID 不能为空")
    }
    return DB.Model(&SubscriptionUsage{}).
        Where("id = ?", usageId).
        Update("used_quota", 0).Error
}

// ResetUsageWindowWithTx:786-796 - 事务版本
func ResetUsageWindowWithTx(tx *gorm.DB, usageId int64) error {
    // ... 实现与上同
}

// AtomicIncrementUsage:798-810 - 原子增量操作
func AtomicIncrementUsage(usageId int64, amount int64) error {
    if usageId == 0 {
        return errors.New("使用量记录 ID 不能为空")
    }
    if amount <= 0 {
        return errors.New("增量必须为正数")
    }
    return DB.Model(&SubscriptionUsage{}).
        Where("id = ?", usageId).
        Update("used_quota", gorm.Expr("used_quota + ?", amount)).Error
}

// AtomicIncrementUsageWithTx:812-824 - 事务版本
func AtomicIncrementUsageWithTx(tx *gorm.DB, usageId int64, amount int64) error {
    // ... 实现与上同
}

// AtomicDecrementUsage:826-854 - 原子减量操作（带边界检查）
func AtomicDecrementUsage(usageId int64, amount int64) error {
    if usageId == 0 {
        return errors.New("使用量记录 ID 不能为空")
    }
    if amount <= 0 {
        return errors.New("减量必须为正数")
    }

    // 使用原子操作防止减成负数
    result := DB.Model(&SubscriptionUsage{}).
        Where("id = ? AND used_quota >= ?", usageId, amount).
        Update("used_quota", gorm.Expr("used_quota - ?", amount))

    if result.Error != nil {
        return result.Error
    }
    if result.RowsAffected == 0 {
        return errors.New("减量失败：额度不足或记录不存在")
    }
    return nil
}

// AtomicDecrementUsageWithTx:856-872 - 事务版本
func AtomicDecrementUsageWithTx(tx *gorm.DB, usageId int64, amount int64) error {
    // ... 实现与上同
}
```

### 修复统计

- **Lines Added:** +160
- **Lines Removed:** -8
- **Files Changed:** 1 (model/subscription_usage.go)
- **Net Change:** +152

### 验证

- ✓ `go build ./model/...` 编译通过
- ✓ 旧方法已弃用并委托给策略感知版本
- ✓ 新增的 Update/Reset/AtomicIncrement 方法符合任务2.2.3规范

---

## 三次修复 (2025-12-08T10:30)

### 修复的问题

#### 1. GetRemainingQuota/GetUsagePercentage 不支持滚动窗口 ✓ 已修复

**问题**: GetRemainingQuota 和 GetUsagePercentage 仍使用 GetUsageBySubscriptionAndPeriod 查询，该方法使用 CalculateWindowBounds（固定窗口）按自然边界查找窗口，未带上 window_strategy。滚动窗口的 window_start 是首次使用时间，这里会返回 nil，导致无法获取正在使用的滚动窗口，违背任务 2.2.8 修复目标

**修复**: 将 GetRemainingQuota 和 GetUsagePercentage 改用 GetActiveUsageWindow

```go
// GetRemainingQuota:207-220 - 改用 GetActiveUsageWindow 支持滚动窗口
func GetRemainingQuota(subscriptionId int64, period string) (int64, error) {
    // 使用 GetActiveUsageWindow 以支持滚动窗口
    usage, err := GetActiveUsageWindow(subscriptionId, period)
    if err != nil {
        return 0, err
    }

    if usage == nil {
        return -1, nil // -1 表示没有活跃窗口（可能是无限额度或未初始化）
    }

    return usage.LimitQuota - usage.UsedQuota, nil
}

// GetUsagePercentage:222-235 - 同样改用 GetActiveUsageWindow
func GetUsagePercentage(subscriptionId int64, period string) (float64, error) {
    usage, err := GetActiveUsageWindow(subscriptionId, period)
    if err != nil {
        return 0, err
    }

    if usage == nil || usage.LimitQuota == 0 {
        return 0, nil
    }

    return float64(usage.UsedQuota) / float64(usage.LimitQuota) * 100, nil
}

// GetUsageBySubscriptionAndPeriod:102-127 - 标记为废弃
// Deprecated: 该方法使用固定窗口边界查询，不支持滚动窗口。
// 请使用 GetActiveUsageWindow 替代。
func GetUsageBySubscriptionAndPeriod(subscriptionId int64, period string) (*SubscriptionUsage, error) {
    // ... 原有实现保持不变，但已标记废弃
}
```

**核心改进**:
- GetActiveUsageWindow 使用 `window_end >= now` 查询，支持滚动窗口
- 滚动窗口的 window_start 由首次使用触发，不依赖固定自然边界
- 固定窗口按自然边界（如每天 00:00）计算，与滚动窗口逻辑不兼容

### 修复统计

- **Lines Added:** +15
- **Lines Removed:** -10
- **Files Changed:** 1 (model/subscription_usage.go)
- **Net Change:** +5

### 验证

- ✓ `go build ./model/...` 编译通过
- ✓ GetRemainingQuota/GetUsagePercentage 现在支持滚动窗口查询
- ✓ 符合任务 2.2.8 的修复要求

---

## 四次修复 (2025-12-08T14:00)

### 修复的问题

#### 1. 移除废弃方法 GetUsageBySubscriptionAndPeriod ✓ 已修复

**问题**: GetUsageBySubscriptionAndPeriod 仍按固定窗口查询（使用 CalculateWindowBounds 和 window_start 精确匹配），未考虑滚动窗口或 window_strategy，在被调用时仍会漏掉滚动窗口（尽管已标记 Deprecated，但任何残余调用都会返回空窗口，影响额度查询/校验）

**调用情况检查**:
```bash
$ grep -rn "GetUsageBySubscriptionAndPeriod(" --include="*.go" .
model/subscription_usage.go:106:func GetUsageBySubscriptionAndPeriod(subscriptionId int64, period string) (*SubscriptionUsage, error) {
```

**结论**: 该方法已无任何调用者，仅存在方法定义

**修复**: 直接移除该废弃方法，避免未来误用

```go
// model/subscription_usage.go:102-127 - 已移除

// 移除前的代码：
// func GetUsageBySubscriptionAndPeriod(subscriptionId int64, period string) (*SubscriptionUsage, error) {
//     if subscriptionId == 0 {
//         return nil, errors.New("订阅 ID 不能为空")
//     }
//
//     now := common.GetTimestamp()
//     windowStart, _ := CalculateWindowBounds(period, now)
//
//     var usage SubscriptionUsage
//     err := DB.Where("subscription_id = ? AND period = ? AND window_start = ?",
//         subscriptionId, period, windowStart).
//         First(&usage).Error
//
//     if err != nil {
//         if errors.Is(err, gorm.ErrRecordNotFound) {
//             return nil, nil
//         }
//         return nil, err
//     }
//
//     return &usage, nil
// }
```

**移除理由**:
1. ❌ **不支持滚动窗口**: 使用固定窗口边界 (CalculateWindowBounds) 和 window_start 精确匹配
2. ❌ **功能已替代**: GetActiveUsageWindow 完全替代其功能，且支持滚动窗口
3. ❌ **无调用者**: 代码库中无任何地方调用该方法
4. ❌ **误用风险**: 保留会导致未来开发者误用，返回空结果

**替代方法**: GetActiveUsageWindow

```go
// GetActiveUsageWindow:68-100 - 推荐使用的替代方法
// 支持滚动窗口和固定窗口，使用 window_end >= now 查询活跃窗口
func GetActiveUsageWindow(subscriptionId int64, period string) (*SubscriptionUsage, error) {
    if subscriptionId == 0 {
        return nil, errors.New("订阅 ID 不能为空")
    }

    now := common.GetTimestamp()
    var usage SubscriptionUsage

    // 查询当前活跃窗口（window_end >= now）
    // 支持滚动窗口（首次使用时创建）和固定窗口（自然边界）
    err := DB.Where("subscription_id = ? AND period = ? AND window_end >= ?",
        subscriptionId, period, now).
        Order("window_start ASC").
        First(&usage).Error

    if err != nil {
        if errors.Is(err, gorm.ErrRecordNotFound) {
            return nil, nil
        }
        return nil, err
    }

    return &usage, nil
}
```

**对比分析**:

| 特性 | GetUsageBySubscriptionAndPeriod (已移除) | GetActiveUsageWindow (推荐) |
|------|------------------------------------------|---------------------------|
| 支持滚动窗口 | ❌ 否 (使用固定边界) | ✅ 是 (使用 window_end >= now) |
| 支持固定窗口 | ✅ 是 | ✅ 是 |
| 查询条件 | window_start 精确匹配 | window_end >= now (活跃窗口) |
| 滚动窗口行为 | 返回 nil (找不到固定边界窗口) | 正确返回首次使用创建的窗口 |
| 当前使用情况 | 无调用者 | GetRemainingQuota, GetUsagePercentage 等 |
| 状态 | 已移除 | 推荐使用 |

### 修复统计

- **Lines Added:** +0
- **Lines Removed:** -26
- **Files Changed:** 1 (model/subscription_usage.go)
- **Net Change:** -26

### 验证

- ✓ `go build ./...` 编译通过
- ✓ 无任何调用者，移除安全
- ✓ GetActiveUsageWindow 完全替代其功能
- ✓ 避免未来误用固定窗口方法导致滚动窗口查询失败

### 影响分析

**移除影响**:
- ✅ **无破坏性变更**: 方法无调用者，移除不影响现有代码
- ✅ **简化代码**: 减少 26 行废弃代码
- ✅ **避免误用**: 防止未来开发者误用不支持滚动窗口的方法
- ✅ **代码质量**: 移除已被替代的冗余方法

**如果保留的风险**:
1. 开发者可能误用 GetUsageBySubscriptionAndPeriod
2. 在滚动窗口场景下返回 nil（查询失败）
3. 导致额度查询/校验失败
4. 增加调试难度和技术债务

**正确的查询方式**:
- ✅ 使用 GetActiveUsageWindow 查询活跃窗口（支持滚动+固定）
- ✅ 使用 GetRemainingQuota 获取剩余额度
- ✅ 使用 GetUsagePercentage 获取使用百分比
- ✅ 使用 AtomicIncrementUsage 原子增加使用量

