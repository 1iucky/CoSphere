# Implementation Log: Task 2.2.1

**Summary:** 实现套餐模型 model/subscription_plan.go，包含 SubscriptionPlan 结构体定义、PlanLimits 限额配置、CRUD 操作、搜索功能和模型白名单验证

**Timestamp:** 2025-12-07T07:16:24.015Z
**Log ID:** 9d2072ec-cd74-4770-bd6a-1cf38f24f269

---

## Statistics

- **Lines Added:** +380
- **Lines Removed:** -0
- **Files Changed:** 1
- **Net Change:** 380

## Files Modified
_No files modified_

## Files Created
- model/subscription_plan.go

---

## Artifacts

### 数据结构

#### SubscriptionPlan 结构体

```go
type SubscriptionPlan struct {
    Id                  int64   `json:"id" gorm:"primaryKey;autoIncrement"`
    SKU                 *string `json:"sku" gorm:"type:varchar(64);uniqueIndex"`
    Name                string  `json:"name" gorm:"type:varchar(128);not null"`
    Description         *string `json:"description" gorm:"type:text"`
    PriceCents          int64   `json:"price_cents" gorm:"not null"`
    Currency            string  `json:"currency" gorm:"type:varchar(8);default:'USD'"`
    BillingCycle        string  `json:"billing_cycle" gorm:"type:varchar(32);not null"`
    BillingCycleValue   int     `json:"billing_cycle_value" gorm:"default:1"`
    AllowWalletFallback bool    `json:"allow_wallet_fallback" gorm:"default:true"`
    Status              string  `json:"status" gorm:"type:varchar(32);default:'draft';index"`
    StartAt             *int64  `json:"start_at" gorm:"bigint"`
    EndAt               *int64  `json:"end_at" gorm:"bigint"`
    ModelWhitelist      *string `json:"model_whitelist" gorm:"type:text"`
    ChannelGroups       *string `json:"channel_groups" gorm:"type:text"`
    Extra               *string `json:"extra" gorm:"type:text"`
    CreatedAt           int64   `json:"created_at" gorm:"bigint;autoCreateTime"`
    UpdatedAt           int64   `json:"updated_at" gorm:"bigint;autoUpdateTime"`
    Limits []SubscriptionPlanLimit `json:"limits" gorm:"foreignKey:PlanId;references:Id"`
}
```

| 字段 | 类型 | 说明 |
|-----|------|------|
| SKU | *string | 套餐唯一标识码 |
| PriceCents | int64 | 价格（分） |
| BillingCycle | string | 计费周期类型 |
| Status | string | 状态：draft/active/archived |
| ModelWhitelist | *string | 模型白名单（JSON） |
| ChannelGroups | *string | 渠道分组（JSON） |

#### SubscriptionPlanLimit 结构体

```go
type SubscriptionPlanLimit struct {
    Id             int64  `json:"id" gorm:"primaryKey;autoIncrement"`
    PlanId         int64  `json:"plan_id" gorm:"not null;uniqueIndex:idx_plan_period,priority:1"`
    Period         string `json:"period" gorm:"type:varchar(32);not null;uniqueIndex:idx_plan_period,priority:2"`
    Quota          int64  `json:"quota" gorm:"not null"`
    Unit           string `json:"unit" gorm:"type:varchar(32);default:'quota'"`
    Enabled        bool   `json:"enabled" gorm:"default:true"`
    WindowStrategy string `json:"window_strategy" gorm:"type:varchar(32);default:'rolling'"`
}
```

| 字段 | 类型 | 说明 |
|-----|------|------|
| Period | string | 周期：five_hours/day/week/month |
| Quota | int64 | 限额值 |
| WindowStrategy | string | 窗口策略：rolling/fixed |

### 实现的方法

#### CRUD 方法
| 方法名 | 签名 | 说明 |
|-------|------|------|
| `CreateSubscriptionPlan` | `(plan *SubscriptionPlan) error` | 创建套餐（含限额），使用事务 |
| `UpdateSubscriptionPlan` | `(plan *SubscriptionPlan) error` | 更新套餐，删除旧限额重建 |
| `GetSubscriptionPlanById` | `(id int64) (*SubscriptionPlan, error)` | 按 ID 获取（含限额） |
| `GetSubscriptionPlanBySKU` | `(sku string) (*SubscriptionPlan, error)` | 按 SKU 获取 |
| `GetAllSubscriptionPlans` | `(startIdx, num int, status string) ([]*SubscriptionPlan, int64, error)` | 分页获取 |
| `GetActiveSubscriptionPlans` | `() ([]*SubscriptionPlan, error)` | 获取已发布可用套餐 |
| `DeleteSubscriptionPlan` | `(id int64) error` | 删除（检查关联订阅） |

#### 状态管理方法
| 方法名 | 签名 | 说明 |
|-------|------|------|
| `PublishSubscriptionPlan` | `(id int64) error` | 发布套餐（draft → active） |
| `UnpublishSubscriptionPlan` | `(id int64) error` | 下架套餐 |
| `ArchiveSubscriptionPlan` | `(id int64) error` | 归档套餐 |

#### 辅助方法
| 方法名 | 签名 | 说明 |
|-------|------|------|
| `GetModelWhitelistAsSlice` | `() ([]string, error)` | 解析模型白名单 |
| `SetModelWhitelistFromSlice` | `(models []string) error` | 设置模型白名单 |
| `IsAvailable` | `() bool` | 检查套餐是否可用 |
| `GetEnabledLimits` | `() []SubscriptionPlanLimit` | 获取启用的限额 |

### 关键实现

#### 事务处理
```go
return DB.Transaction(func(tx *gorm.DB) error {
    if err := tx.Create(plan).Error; err != nil {
        return err
    }
    if len(plan.Limits) > 0 {
        if err := tx.Create(&plan.Limits).Error; err != nil {
            return err
        }
    }
    return nil
})
```

### 计费周期类型
- `monthly` / `yearly` / `custom`
- `five_hours` / `day` / `week` / `month`

### 依赖常量
- `common.PlanStatusDraft` / `Active` / `Archived`
- `common.BillingCycleMonthly` / `Yearly` 等
- `common.LimitPeriodFiveHours` / `Day` / `Week` / `Month`
- `common.WindowStrategyRolling` / `Fixed`

---

## 二次修复 (2025-12-08T02:26)

### 修复的问题

#### 1. 名称唯一性校验缺失 ✓ 已修复

**问题**: CreateSubscriptionPlan 和 UpdateSubscriptionPlan 未校验套餐名称唯一性，违反需求 FR-1 "名称唯一"

**修复**:
- 在 `CreateSubscriptionPlan` 中添加名称存在性检查
- 在 `UpdateSubscriptionPlan` 中添加名称冲突检查（排除自己）

```go
// CreateSubscriptionPlan:67-74
var count int64
if err := DB.Model(&SubscriptionPlan{}).Where("name = ?", plan.Name).Count(&count).Error; err != nil {
    return err
}
if count > 0 {
    return errors.New("套餐名称已存在")
}

// UpdateSubscriptionPlan:108-115
var count int64
if err := DB.Model(&SubscriptionPlan{}).Where("name = ? AND id != ?", plan.Name, plan.Id).Count(&count).Error; err != nil {
    return err
}
if count > 0 {
    return errors.New("套餐名称已存在")
}
```

#### 2. 价格验证错误 ✓ 已修复

**问题**: 价格验证为 `< 0`，允许创建0元套餐，违反需求 FR-1 "价格 > 0"

**修复**: 改为 `<= 0`，确保价格必须 > 0

```go
// validateSubscriptionPlan:346
// 修改前: if plan.PriceCents < 0
// 修改后:
if plan.PriceCents <= 0 {
    return errors.New(common.MsgPlanPriceInvalid)
}
```

#### 3. 模型/渠道白名单验证缺失 ✓ 已修复

**问题**: 未验证 ModelWhitelist 和 ChannelGroups 的 JSON 格式有效性，违反任务2.2.1"模型白名单验证"目标

**修复**: 添加格式验证

```go
// validateSubscriptionPlan:369-381
// 验证模型白名单格式
if plan.ModelWhitelist != nil && *plan.ModelWhitelist != "" {
    if _, err := plan.GetModelWhitelistAsSlice(); err != nil {
        return errors.New("模型白名单格式无效")
    }
}

// 验证渠道分组格式
if plan.ChannelGroups != nil && *plan.ChannelGroups != "" {
    if _, err := plan.GetChannelGroupsAsSlice(); err != nil {
        return errors.New("渠道分组格式无效")
    }
}
```

### 修复统计

- **Lines Added:** +30
- **Lines Removed:** -2
- **Files Changed:** 1 (model/subscription_plan.go)
- **Net Change:** +28

### 验证

- ✓ `go build ./model/...` 编译通过
- ✓ 符合需求 FR-1 要求
- ✓ 符合任务2.2.1规范
