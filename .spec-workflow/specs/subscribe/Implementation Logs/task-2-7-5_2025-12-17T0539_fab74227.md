# Implementation Log: Task 2.7.5

**Summary:** 实现并存逻辑（coexist选项）：executeCoexistRedemption 创建新订阅保留旧订阅，检查用户订阅数量限制（SUBSCRIPTION_MAX_PER_USER），初始化订阅优先级（动态插入）

**Timestamp:** 2025-12-17T05:39:41.080Z
**Log ID:** fab74227-5fcc-425b-a838-f30dedff5ad6

---

## Statistics

- **Lines Added:** +75
- **Lines Removed:** -0
- **Files Changed:** 1
- **Net Change:** 75

## Files Modified
- service/redemption_service.go

## Files Created
_No files created_

---

## Artifacts

### Functions

#### executeCoexistRedemption
- **Purpose:** 执行并存兑换：检查订阅数量限制 → 创建新订阅（保留旧订阅）→ 初始化优先级 → 标记兑换码 → 记录账单和审计
- **Location:** service/redemption_service.go:761
- **Signature:** (s *RedemptionService) executeCoexistRedemption(tx *gorm.DB, result *RedemptionResult, userId int64, redemption *model.Redemption, plan *model.SubscriptionPlan, payload *model.RedemptionSubscriptionPayload) error
- **Exported:** No

#### executeReplaceRedemption
- **Purpose:** 执行替换兑换：取消旧订阅 → 创建新订阅（不做价值折算）→ 初始化优先级 → 记录账单和审计
- **Location:** service/redemption_service.go:837
- **Signature:** (s *RedemptionService) executeReplaceRedemption(tx *gorm.DB, result *RedemptionResult, userId int64, redemption *model.Redemption, plan *model.SubscriptionPlan, payload *model.RedemptionSubscriptionPayload, existingPlanId int64) error
- **Exported:** No

#### getUserSubscriptionByPlanWithTx
- **Purpose:** 在事务中获取用户指定套餐的订阅
- **Location:** service/redemption_service.go:1097
- **Signature:** (s *RedemptionService) getUserSubscriptionByPlanWithTx(tx *gorm.DB, userId int64, planId int64) (*model.Subscription, error)
- **Exported:** No

### Integrations

#### Integration
- **Description:** 并存兑换流程：检查订阅数量限制 → 创建新订阅 → 初始化优先级（按 end_at 动态插入）→ 两个订阅同时生效
- **Frontend Component:** RedemptionConflictModal
- **Backend Endpoint:** POST /api/user/redemptions/use?redeem_option=coexist
- **Data Flow:** 用户选择 coexist → 检查限制 → 创建新订阅 → 返回新订阅信息 → 用户拥有多个活跃订阅

#### Integration
- **Description:** 替换兑换流程：取消旧订阅（不退款）→ 创建新订阅 → 初始化优先级
- **Frontend Component:** RedemptionConflictModal
- **Backend Endpoint:** POST /api/user/redemptions/use?redeem_option=replace
- **Data Flow:** 用户选择 replace → 取消旧订阅 → 创建新订阅 → 旧订阅剩余价值作废

