# Implementation Log: Task 2.2.4

**Summary:** 实现订单模型 model/subscription_order.go，包含订单生命周期管理、价格计算、套餐/优惠券快照保存、订单统计等功能

**Timestamp:** 2025-12-07T07:16:24.691Z
**Log ID:** 51180a83-4ca0-4153-92da-26238c69f8a6

---

## Statistics

- **Lines Added:** +520
- **Lines Removed:** -0
- **Files Changed:** 1
- **Net Change:** 520

## Files Modified
_No files modified_

## Files Created
- model/subscription_order.go

---

## Artifacts

### 数据结构

#### SubscriptionOrder 结构体

```go
type SubscriptionOrder struct {
    Id              int64   `json:"id" gorm:"primaryKey;autoIncrement"`
    UserId          int64   `json:"user_id" gorm:"not null;index"`
    PlanId          int64   `json:"plan_id" gorm:"not null;index"`
    PlanSnapshot    string  `json:"plan_snapshot" gorm:"type:text;not null"`
    CouponId        *int64  `json:"coupon_id" gorm:"index"`
    CouponSnapshot  *string `json:"coupon_snapshot" gorm:"type:text"`
    PaymentChannel  string  `json:"payment_channel" gorm:"type:varchar(32);not null"`
    PriceCents      int64   `json:"price_cents" gorm:"bigint;not null"`
    DiscountCents   int64   `json:"discount_cents" gorm:"bigint;default:0"`
    FinalPriceCents int64   `json:"final_price_cents" gorm:"bigint;not null"`
    RedemptionId    *int64  `json:"redemption_id" gorm:"index"`
    BillId          *int64  `json:"bill_id" gorm:"index"`
    Status          string  `json:"status" gorm:"type:varchar(32);default:'pending';index"`
}
```

| 字段 | 类型 | 说明 |
|-----|------|------|
| PlanSnapshot | string | 套餐快照（JSON） |
| CouponSnapshot | *string | 优惠券快照（JSON） |
| PaymentChannel | string | 支付渠道 |
| PriceCents | int64 | 原价（分） |
| DiscountCents | int64 | 折扣金额（分） |
| FinalPriceCents | int64 | 最终价格（分） |

#### PlanSnapshotData 结构体

```go
type PlanSnapshotData struct {
    Id                  int64                   `json:"id"`
    SKU                 string                  `json:"sku"`
    Name                string                  `json:"name"`
    PriceCents          int64                   `json:"price_cents"`
    BillingCycle        string                  `json:"billing_cycle"`
    Limits              []PlanLimitSnapshotData `json:"limits"`
}
```

#### CouponSnapshotData 结构体

```go
type CouponSnapshotData struct {
    Id            int64  `json:"id"`
    Code          string `json:"code"`
    DiscountType  string `json:"discount_type"`
    DiscountValue int64  `json:"discount_value"`
}
```

### 实现的方法

#### CRUD 方法
| 方法名 | 签名 | 说明 |
|-------|------|------|
| `CreateSubscriptionOrder` | `(order *SubscriptionOrder) error` | 创建订单 |
| `GetSubscriptionOrderById` | `(id int64) (*SubscriptionOrder, error)` | 按 ID 获取 |
| `GetSubscriptionOrderByIdWithTx` | `(tx *gorm.DB, id int64, forUpdate bool) (*SubscriptionOrder, error)` | 事务中获取（行锁） |
| `GetSubscriptionOrdersByUser` | `(userId int64, startIdx, num int, status string) ([]*SubscriptionOrder, int64, error)` | 用户订单 |
| `GetPendingOrders` | `(timeoutSeconds int64, limit int) ([]*SubscriptionOrder, error)` | 超时待支付订单 |

#### 状态转换方法
| 方法名 | 签名 | 说明 |
|-------|------|------|
| `PayOrder` | `(id int64, billId int64) error` | 支付订单 |
| `CancelOrder` | `(id int64) error` | 取消订单 |
| `ExpireOrder` | `(id int64) error` | 过期订单 |
| `RefundOrder` | `(id int64) error` | 退款订单 |

#### 快照方法
| 方法名 | 签名 | 说明 |
|-------|------|------|
| `CreatePlanSnapshot` | `(plan *SubscriptionPlan) (string, error)` | 创建套餐快照 |
| `GetPlanSnapshotData` | `() (*PlanSnapshotData, error)` | 解析套餐快照 |
| `CreateCouponSnapshot` | `(coupon *Coupon) (string, error)` | 创建优惠券快照 |
| `GetCouponSnapshotData` | `() (*CouponSnapshotData, error)` | 解析优惠券快照 |

#### 统计方法
| 方法名 | 签名 | 说明 |
|-------|------|------|
| `CountOrdersByUser` | `(userId int64, status string) (int64, error)` | 统计用户订单数 |
| `SumOrderAmountByUser` | `(userId int64) (int64, error)` | 用户已支付总金额 |
| `GetOrderStatsByPlan` | `(planId int64) (paidCount, totalRevenue int64, error)` | 套餐订单统计 |

### 关键实现

#### 快照创建
```go
func CreatePlanSnapshot(plan *SubscriptionPlan) (string, error) {
    modelWhitelist, _ := plan.GetModelWhitelistAsSlice()
    var limits []PlanLimitSnapshotData
    for _, limit := range plan.GetEnabledLimits() {
        limits = append(limits, PlanLimitSnapshotData{...})
    }
    snapshot := PlanSnapshotData{...}
    data, _ := json.Marshal(snapshot)
    return string(data), nil
}
```

### 状态机
```
pending → paid → refunded
    ↓
cancelled/expired
```

### 支付渠道
- `wallet` - 余额支付
- `redemption` - 兑换码支付
- `alipay` / `wechat` / `stripe` - 第三方支付
- `free` - 免费（0元订单）

### 依赖常量
- `common.OrderStatusPending` / `Paid` / `Cancelled` / `Expired` / `Refunded`
- `common.PaymentChannelWallet` / `Redemption` / `Alipay` / `Wechat` / `Stripe` / `Free`
